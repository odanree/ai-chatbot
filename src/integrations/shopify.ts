// Shopify API client stub
export async function getProductInfo(productId: string) {
  // TODO: Fetch product info from Shopify Storefront/Admin API
  return { id: productId, name: 'Sample Product' };
}
